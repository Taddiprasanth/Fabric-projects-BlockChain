# Personal Credentials Configuration
# Update these values with your own information

# Personal Information
user.name=Your Name
user.email=your.email@example.com
user.organization=Your Organization
user.role=Developer

# Application Credentials
app.name=MyCustomAssetContract
app.version=1.0.0
app.description=Customized Asset Management Contract

# Database/Connection Settings (if needed)
db.host=localhost
db.port=5432
db.name=myassetdb
db.username=dbuser
db.password=dbpassword

# API Keys (if needed)
api.key=your-api-key-here
api.secret=your-api-secret-here

# Security Settings
encryption.key=your-encryption-key
signature.key=your-signature-key

# Custom Business Logic
business.rules.enabled=true
max.asset.value=1000000
min.asset.value=1
